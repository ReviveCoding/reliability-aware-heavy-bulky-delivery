# Resume and Interview Evidence

## Safe primary bullet

- Built a reproducible semi-synthetic heavy-bulky delivery planning benchmark connecting LightGBM quantile forecasts, Reference-Aligned Service State estimates, five route-generation strategies, vehicle/crew/skill constraints, bounded CP-SAT challengers, common-scenario replay, SQL marts, artifact integrity, and reliability promotion gates.

## Safe engineering bullet

- Validated 64 automated tests and source, wheel, sdist, M5-pattern, and multi-worker API paths; the frozen full run processed 14,141 historical and 206 planning orders, generated 404 route candidates, served every order with zero hard-constraint violations, and retained the simpler feasible policy when advanced challengers failed promotion evidence.

## Safe operational bullet

- Added PID/token publication locks, dead and malformed-lock recovery, stable decision fingerprints, per-file SHA-256 manifests, fail-closed CLI/API output verification, and a real two-worker Uvicorn concurrency smoke yielding one successful run and one HTTP 409 conflict.

## Avoid

Do not claim Amazon internal data, actual planner adoption, production-scale deployment, causal savings, optimality when status is only feasible, CUDA execution, or Chronos results without separate evidence.
